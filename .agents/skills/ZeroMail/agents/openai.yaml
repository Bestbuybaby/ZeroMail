interface:
  display_name: "ZeroMail"
  short_description: "Repo-specific patterns and workflows for ZeroMail"
  default_prompt: "Use the ZeroMail repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true